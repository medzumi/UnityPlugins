using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SceneManagament.Examples
{
    public class LoadDefault : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            new ExampleScene();
        }
    }
}
